# Dependencies
node_modules
.pnpm-store
.pnpm-debug.log*
.npm
.yarn
# Build outputs
dist
build
out
.ladle/dist
.ladle/build
*.tsbuildinfo

# Test / coverage
coverage
.nyc_output
*.lcov

# Audit tooling output (analysis databases, reports, evidence)
clear-project-output
code-review-output

# Caches
.turbo
.vite
.cache
.parcel-cache
.swc
.eslintcache
.biome-cache

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
desktop.ini
*~

# Editor / IDE
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
*.swp
*.swo
*.code-workspace

# Env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Optional Theo / project-specific scratch
.scratch
.tmp

# Backups, temporary build artefacts, merge residue
*.bak
*.tmp
*.orig
*.rej

# Local agent / AI tool configuration
.claude/
.theokit/
.aider*

# Local-only scratch clones and scratch tooling — never committed
referencia/
.gstack/

# npm pack artifacts (publint creates these during quality:gates)
*.tgz
packages/ui/src/styles/node_modules/
test-results/
.npmrc

# Python bytecode
__pycache__/
*.pyc

# Design-sync working files
.ds-sync/
ds-bundle/
.design-sync/

# Generated at CSS precompile time (build-precompiled-css.ts) — never committed
packages/ui/src/styles/components-entry.generated.css

# The write root: per-run output from the maintenance kit, never this package's source.
# `records-location.md` makes it the one place cycles write, so it is ignored whole —
# a tracked file here would put one machine's run history in everyone's diff.
.squad/
